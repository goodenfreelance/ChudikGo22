# Полное физико-математическое руководство по биомеханике и динамике "Чудиков" (Grid Creatures)

Настоящий документ содержит всеобъемлющее теоретическое, математическое и алгоритмическое описание физического движка игры **"Чудики на сетке"** (как серверной части на Go с частотой симуляции 100 FPS, так и клиентского движка интерполяции и отрисовки).

---

## Оглавление
1. [Анатомическая модель и дискретизация тела](#1-анатомическая-модель-и-дискретизация-тела)
2. [Тензор инерции Гюйгенса — Штейнера и центр масс](#2-тензор-инерции-гюйгенса--штейна-и-центр-масс)
3. [Биомеханика мышц: Пружинно-демпферная модель Гука и Кельвина — Фойгта](#3-биомеханика-мышц-пружинно-демпферная-модель-гука-и-кельвина--фойгта)
4. [Гидродинамика вязкой среды и модель спина](#4-гидродинамика-вязкой-среды-и-модель-спина)
5. [Интегрирование уравнений движения (Semi-implicit Euler)](#5-интегрирование-уравнений-движения-semi-implicit-euler)
6. [Метаболизм, ускорение (Dash / Boost) и условия расхода еды](#6-метаболизм-ускорение-dash--boost-и-условия-расхода-еды)
7. [Детекция столкновений: Адаптивный CCD и Narrow-Phase SAT](#7-детекция-столкновений-адаптивный-ccd-и-narrow-phase-sat)
8. [Динамика соударений: Угловой и линейный импульс](#8-динамика-соударений-угловой-и-линейный-импульс)
9. [Спящие острова (Sleeping Islands) и пространственный хэш (Spatial Hash Grid)](#9-спящие-острова-sleeping-islands-и-пространственный-хэш-spatial-hash-grid)
10. [Иерархия рендеринга и расчет динамических границ](#10-иерархия-рендеринга-и-расчет-динамических-границ)

---

## 1. Анатомическая модель и дискретизация тела

Чудик представляет собой плоскую дискретно-сочлененную структуру на двумерной декартовой сетке. Тело чудика задается набором анатомических узлов и связей $E = \{e_1, e_2, \dots, e_N\}$ в локальной системе координат $(\Delta x_i, \Delta y_i) \in \mathbb{Z}^2$.

### 1.1. Классификация и весовые характеристики элементов
Каждый тип элемента наделен физической массой $m_i$ и функциональным назначением:

| Тип элемента | Идентификатор | Масса $m_i$ | Описание и физическая роль |
| :--- | :--- | :---: | :--- |
| **Голова** | `head` | **2.0** | Сенсорный центр, челюстной аппарат для захвата пищи, точка задания вектора взгляда $\hat{f}_{head}$ |
| **Шарнир (сустав)** | `joint` | **1.0** | Опорный узел скелета, ось вращения для прилегающих сегментов тела |
| **Левое ребро** | `edge-left` | **0.5** | Каркасный силовой элемент, левое стабилизирующее крыло/плавник |
| **Правое ребро** | `edge-right` | **0.5** | Каркасный силовой элемент, правое стабилизирующее крыло/плавник |
| **Прямая мышца** | `muscle-straight`| **0.8** | Линейный силовой привод (сокращение вдоль оси) |
| **Диагональная мышца** | `muscle-diagonal`| **0.8** | Диагональный привод сгибания/скручивания |

Полная масса чудика:
$$M = \sum_{i=1}^{N} m_i$$

---

## 2. Тензор инерции Гюйгенса — Штейнера и центр масс

### 2.1. Локальный центр масс (Center of Mass, CoM)
Положение центра масс вычисляется через взвешенное среднее радиус-векторов всех элементов:
$$X_{CoM} = \frac{1}{M} \sum_{i=1}^{N} m_i \cdot x_i, \quad Y_{CoM} = \frac{1}{M} \sum_{i=1}^{N} m_i \cdot y_i$$

### 2.2. Момент инерции относительно центра масс ($I_{zz}$)
По теореме Гюйгенса — Штейнера (параллельный перенос осей) для плоской системы точечных масс момент инерции относительно оси $Z$, проходящей через $CoM$, равен:
$$I_{zz} = \sum_{i=1}^{N} m_i \cdot r_i^2 = \sum_{i=1}^{N} m_i \left( (x_i - X_{CoM})^2 + (y_i - Y_{CoM})^2 \right)$$

#### Физическое следствие:
- **Компактные чудики** (элементы сосредоточены близко к $CoM$): $I_{zz} \to \min$, обеспечивая сверхвысокую угловую отзывчивость и мгновенные развороты.
- **Широкие / вытянутые чудики** (тяжелые головы или суставы на длинных плечах): $I_{zz} \gg 1$, что дает высокую курсовую стабильность при прямолинейном движении, но требует больше времени на изменение траектории.

---

## 3. Биомеханика мышц: Пружинно-демпферная модель Гука и Кельвина — Фойгта

Мышцы чудика функционируют как биомеханические актюаторы, совершающие гармонические сокращения с циклической фазой $\phi_j$.

### 3.1. Кинематика сокращения мышцы
Длина мышечного волокна $L_j(t)$ во времени изменяется по закону:
$$L_j(t) = L_{0,j} \cdot \Big(1.0 - \Delta L_{max} \cdot \sin^2(\omega t + \phi_j)\Big)$$
где:
- $L_{0,j}$ — длина в расслабленном состоянии;
- $\Delta L_{max} = 0.35$ — максимальный коэффициент сокращения (35%);
- $\omega = 2\pi \cdot f$ — циклическая частота сокращений.

### 3.2. Уравнение силы Кельвина — Фойгта
Сила, генерируемая мышечным сегментом:
$$\vec{F}_j = -k \big(L_j(t) - L_{0,j}\big) \hat{u}_j - c \big(\vec{v}_{rel} \cdot \hat{u}_j\big) \hat{u}_j$$
где:
- $k$ — модуль упругости Юнга (жесткость пружины);
- $c$ — коэффициент внутреннего вязкоупругого демпфирования;
- $\hat{u}_j$ — единичный вектор ориентации мышечного волокна;
- $\vec{v}_{rel}$ — относительная скорость концов мышечного волокна.

### 3.3. Разделение тяги и вращающего момента (Правило плеч)
Каждый шарнир разделяет тело на левое ($m_{left}$) и правое ($m_{right}$) плечо:
1. **Правило легкого плеча:** Сгибается сторона с меньшей инерционной массой:
   $$m_{active} = \min(m_{left}, m_{right})$$
2. **Симметричная тяга вперед:**
   $$F_{thrust} = \sum_{j} (\vec{F}_j \cdot \hat{f}_{head})$$
3. **Результирующий крутящий момент (Torque):**
   $$\tau_{net} = \sum_{j} \left( (\vec{r}_j - \vec{r}_{CoM}) \times \vec{F}_j \right)_z$$

Если моменты слева и справа уравновешены ($\tau_{left} \approx \tau_{right}$), чудик движется прямо с максимальным КПД. При асимметрии избыток момента переходит в непрерывное вращение $\Delta \theta$.

---

## 4. Гидродинамика вязкой среды и модель спина

Движение чудиков происходит в вязкой квазижидкой среде (микробиологическая чашка Петри).

### 4.1. Линейное и квадратичное гидродинамическое сопротивление (Drag Force)
$$\vec{F}_{drag} = -\gamma_{Stokes} \vec{v} - \frac{1}{2} C_d \rho A_{eff}(\theta, \vec{v}) |\vec{v}| \vec{v}$$
где:
- $\gamma_{Stokes}$ — линейное стоксово трение покоя/малых скоростей;
- $C_d$ — коэффициент аэро-/гидродинамического сопротивления формы ($0.45 - 0.85$);
- $\rho$ — плотность среды;
- $A_{eff}$ — эффективная площадь миделева сечения, зависящая от угла атаки.

### 4.2. Ротационное сопротивление (Rotational Drag / Spin Damping)
Тормозящий вращение момент среды пропорционален угловой скорости $\omega = \dot{\theta}$ и моменту инерции:
$$\tau_{drag} = -\beta_{rot} \cdot I_{zz} \cdot \omega$$

---

## 5. Интегрирование уравнений движения (Semi-implicit Euler)

Физический сервер на Go производит дискретное интегрирование шагом $\Delta t = 0.01$ сек ($100\text{ FPS}$).

### 5.1. Угловой блок (Rotation Integration)
1. Угловое ускорение:
   $$\alpha(t) = \frac{\tau_{net} + \tau_{drag}}{I_{zz}}$$
2. Угловая скорость:
   $$\omega(t + \Delta t) = \omega(t) + \alpha(t) \Delta t$$
3. Угол ориентации:
   $$\theta(t + \Delta t) = \text{normalizeAngle}\big(\theta(t) + \omega(t + \Delta t) \Delta t\big)$$

### 5.2. Трансляционный блок (Translation Integration)
1. Суммарная сила:
   $$\vec{F}_{total} = \vec{F}_{thrust} + \vec{F}_{drag} + \vec{F}_{collision}$$
2. Линейное ускорение:
   $$\vec{a}(t) = \frac{\vec{F}_{total}}{M}$$
3. Линейная скорость:
   $$\vec{v}(t + \Delta t) = \vec{v}(t) + \vec{a}(t) \Delta t$$
4. Координаты в мире:
   $$\vec{r}(t + \Delta t) = \vec{r}(t) + \vec{v}(t + \Delta t) \Delta t$$

---

## 6. Метаболизм, ускорение (Dash / Boost) и условия расхода еды

Ускорение (рывок) представляет собой форсированный режим работы мышц, требующий биологического субстрата (съеденной еды).

### 6.1. Логическое условие активности ускорения
Рывок может быть активен **только и исключительно при положительном балансе еды**:
$$\text{IsDashing}(t) \Longleftrightarrow \big(\text{Input}_{\text{Space}} == \text{true}\big) \land \big(E_{food}(t) > 0\big)$$

**Абсолютные инварианты движка:**
1. Если $E_{food} = 0$, вызов ускорения блокируется.
2. Если в процессе рывка $E_{food}$ падает до $0$, режим `dashing` мгновенно прерывается ($\text{IsDashing} \leftarrow \text{false}$, $\text{State} \leftarrow \text{"moving"}$), а множитель скорости сбрасывается к $1.0\times$.

### 6.2. Скорость сгорания питательных веществ
Расход еды при рывке составляет $2.0$ единицы в секунду:
$$E_{food}(t + \Delta t) = \max\Big(0, E_{food}(t) - \dot{q}_{dash} \cdot \Delta t\Big), \quad \dot{q}_{dash} = 2.0\text{ ед/сек}$$

### 6.3. Модификаторы движения в режиме Dash
При активном рывке применяются мультипликаторы:
- **Линейная тяга:** $v_{forward} = v_{base} \times 1.60$
- **Скорость разворота (маневренность):** $\omega_{turn} = \omega_{base} \times 1.30$

---

## 7. Детекция столкновений: Адаптивный CCD и Narrow-Phase SAT

Для исключения эффекта туннелирования («просачивания» сквозь стены и чудиков на скорости $1.6\times$) реализована двухфазная система детекции.

### 7.1. Broad-Phase (Spatial Hashing)
Мир квантуется ячейками размера $W_{cell} = 64$ пикселя. Поиск потенциальных коллизий выполняется за время $O(1)$ на объект вместо квадратичного $O(N^2)$.

### 7.2. Continuous Collision Detection (CCD)
Для быстро движущихся тел траектория за шаг $\Delta t$ моделируется как капсула (Swept-Circle / Swept-AABB). Проверяется пересечение луча движения со сферой радиуса $R_A + R_B$:
$$|\vec{r}_A(t^*) - \vec{r}_B(t^*)| = R_A + R_B, \quad t^* \in [0, \Delta t]$$

### 7.3. Narrow-Phase (Поэлементная точная проверка)
Вычисляются абсолютные мировые координаты каждого узла чудика $k$ с учетом матрицы преобразования $T(\vec{r}, \theta)$:
$$\begin{pmatrix} x_{world, k} \\ y_{world, k} \end{pmatrix} = \begin{pmatrix} X_{CoM} \\ Y_{CoM} \end{pmatrix} + \begin{pmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{pmatrix} \begin{pmatrix} \Delta x_k - X_{CoM,loc} \\ \Delta y_k - Y_{CoM,loc} \end{pmatrix}$$

Находится минимальное расстояние между деталями $A$ и $B$:
$$d_{min} = \min_{i \in A, j \in B} \|\vec{p}_i - \vec{p}_j\|$$
Если $d_{min} < (r_i + r_j)$, генерируется контактная пара (Contact Manifold).

---

## 8. Динамика соударений: Угловой и линейный импульс

При соударении двух чудиков $A$ и $B$ генерируется мгновенный векторный импульс реакции $J \cdot \hat{n}$.

### 8.1. Величина импульса нормальной реакции
$$J = \frac{-(1 + e) \cdot (\vec{v}_{rel} \cdot \hat{n})}{\frac{1}{M_A} + \frac{1}{M_B} + \frac{(\vec{r}_{A,contact} \times \hat{n})^2}{I_{zz,A}} + \frac{(\vec{r}_{B,contact} \times \hat{n})^2}{I_{zz,B}}}$$
где:
- $e \in [0, 1]$ — коэффициент восстановления (эластичности удара, по умолчанию $0.5$);
- $\vec{v}_{rel} = (\vec{v}_A + \vec{\omega}_A \times \vec{r}_A) - (\vec{v}_B + \vec{\omega}_B \times \vec{r}_B)$ — относительная скорость в точке контакта;
- $\hat{n}$ — единичная нормаль удара.

### 8.2. Изменение линейных и угловых скоростей
$$\vec{v}_A' = \vec{v}_A + \frac{J \hat{n}}{M_A}, \quad \vec{v}_B' = \vec{v}_B - \frac{J \hat{n}}{M_B}$$
$$\omega_A' = \omega_A + \frac{(\vec{r}_A \times J\hat{n})_z}{I_{zz,A}}, \quad \omega_B' = \omega_B - \frac{(\vec{r}_B \times J\hat{n})_z}{I_{zz,B}}$$

### 8.3. Позиционное расталкивание (Anti-penetration solver)
Для устранения взаимного перекрытия тел на глубину проникновения $p = (r_A + r_B) - d_{min} > 0$:
$$\Delta \vec{r}_A = -\hat{n} \cdot p \cdot \frac{M_B}{M_A + M_B}, \quad \Delta \vec{r}_B = +\hat{n} \cdot p \cdot \frac{M_A}{M_A + M_B}$$

---

## 9. Спящие острова (Sleeping Islands) и пространственный хэш (Spatial Hash Grid)

Для обеспечения масштабируемости до сотен активных чудиков и тысяч единиц пищи:

### 9.1. Условие засыпания чудика (Sleep Condition)
Чудик переходит в спящий режим (`isSleeping = true`), если его суммарная кинетическая энергия ниже пороговой $\varepsilon_{sleep}$:
$$E_k = \frac{1}{2} M |\vec{v}|^2 + \frac{1}{2} I_{zz} \omega^2 < \varepsilon_{sleep} \approx 10^{-4}\text{ Дж}$$
и отсутствуют активные мышечные сокращения.

### 9.2. Пробуждение (Wake-up trigger)
Чудик моментально просыпается при:
- Появлении внешнего импульса соударения ($J > 0$);
- Поступлении управляющей команды от игрока или AI-мозга бота;
- Обнаружении съедобного ресурса в радиусе видимости головы.

---

## 10. Иерархия рендеринга и стабильный расчет динамических границ (Smooth Smart HUD)

Для гарантированного предотвращения наложения информационных надписей (HUD, имя, масса, скорость, еда, полоска энергии) на тело чудика и **исключения циклического дергания (jitter) при взмахах крыльев** используется комбинированный алгоритм **Rest-Pose Envelope + Exponential LERP Smoothing**:

### 10.1. Статический охват скелета с запасом на максимальный вылет (Rest-Pose Envelope)
Вместо мгновенного положения деформированных мышц/крыльев, изменяющихся с высокой частотой на каждом тике анимации, для каждого узла используются базовые координаты ненапряженного скелета $(x_{loc, i}, y_{loc, i})$, дополненные максимальным геометрическим вылетом крыла $R_{wing\_max} \approx 0.72 \cdot W_{cell}$:
$$y_{top, i} = x_{loc, i} \cdot \sin\theta + y_{loc, i} \cdot \cos\theta - R_{wing\_max}$$

Целевая верхняя граница:
$$Y_{target} = \min\Big(\min_{i=1 \dots N}(y_{top, i}), Y_{selection}, Y_{dash\_flame}\Big)$$

### 10.2. Экспоненциальное сглаживание по времени (Exponential Damping)
Для абсолютно плавного перемещения плашки HUD при разворотах чудика применяется независимое от FPS сглаживание:
$$Y_{HUD}(t + \Delta t) = Y_{HUD}(t) + \big(Y_{target} - Y_{HUD}(t)\big) \cdot \big(1 - e^{-16 \cdot \Delta t}\big)$$

Благодаря этому:
- **Полностью устранены рывки и прыжки** HUD при ритмичных взмахах крыльев и сжатиях мышц;
- При поворотах надпись мягко и плавно скользит на безопасную высоту;
- Текст и полоска энергии гарантированно парят строго над чудиком при любой конфигурации тела.
